using Amazon.CDK;
using Constructs;

namespace Apprunner
{
    public class ApprunnerStack : Stack
    {
        internal ApprunnerStack(Construct scope, string id, IStackProps props = null) : base(scope, id, props)
        {
            // The code that defines your stack goes here
        }
    }
}
